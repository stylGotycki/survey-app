package net.domaszk.survey.user.persistence.id;

/// This package contains UserId exposed to the other modules. It's for valid referencing since I cannot just "share"
/// User business logic across the modules. Damn DDD...
